// export const isRefObject = <T extends ForwardedRef<P>, P extends HTMLElement>(
//   arg: unknown,
// ): arg is RefObject<P> => {
//   const ref = arg as RefObject<P>;

//   return ref.current?.nodeName ===
// };
